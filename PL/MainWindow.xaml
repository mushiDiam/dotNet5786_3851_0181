<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Main Window &amp; Configuration" Height="550" Width="900"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock Text="Time Simulation" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="5" Margin="0,0,0,15" Background="#F0F0F0">
                <TextBlock Text="{Binding CurrentClock}" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"/>
            </Border>

            <Button Content="+1 Minute" Command="{Binding AdvanceMinuteCommand}" Margin="0,5" Padding="5"/>
            <Button Content="+1 Hour" Command="{Binding AdvanceHourCommand}" Margin="0,5" Padding="5"/>
            <Button Content="+1 Day" Command="{Binding AdvanceDayCommand}" Margin="0,5" Padding="5"/>
            <Button Content="+1 Month" Command="{Binding AdvanceMonthCommand}" Margin="0,5" Padding="5"/>
            <Button Content="+1 Year" Command="{Binding AdvanceYearCommand}" Margin="0,5" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10,0">
            <TextBlock Text="Management" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <Button Content="Initialize Database" Command="{Binding InitializeDB}" Margin="0,5" Padding="10" Background="#DDDDFF"/>
            <Button Content="Reset Database" Command="{Binding ResetDB}" Margin="0,5" Padding="10" Background="#FFDDDD"/>

            <Separator Margin="0,20"/>

            <Button Content="List of Couriers" Command="{Binding CourierList}" Margin="0,5" Padding="10"/>
        </StackPanel>

        <GroupBox Grid.Column="2" Header="Configuration Settings" Margin="10,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="5">

                    <Label Content="Company Address:"/>
                    <TextBox Text="{Binding Config.CompanyAddress}" Padding="3"/>

                    <Label Content="Manager ID:"/>
                    <TextBox Text="{Binding Config.ManagerId}" Padding="3"/>

                    <Label Content="Manager Password:"/>
                    <TextBox Text="{Binding Config.ManagerPassword}" Padding="3"/>

                    <Label Content="Maximum Air Range (km):"/>
                    <TextBox Text="{Binding Config.MaximumAirRange}" Padding="3"/>

                    <Label Content="Maximum Distance (km):"/>
                    <TextBox Text="{Binding Config.MaxiumDistance}" Padding="3"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <Label Content="Latitude:"/>
                            <TextBox Text="{Binding Config.Latitude}" Padding="3"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <Label Content="Longitude:"/>
                            <TextBox Text="{Binding Config.Longitude}" Padding="3"/>
                        </StackPanel>
                    </Grid>

                    <Label Content="Avg Car Speed:"/>
                    <TextBox Text="{Binding Config.AverageCarSpeed}" Padding="3"/>

                    <Label Content="Avg Motorbike Speed:"/>
                    <TextBox Text="{Binding Config.AverageMotorbikeSpeed}" Padding="3"/>

                    <Label Content="Avg Bike Speed:"/>
                    <TextBox Text="{Binding Config.AverageBikeSpeed}" Padding="3"/>

                    <Label Content="Avg Walking Speed:"/>
                    <TextBox Text="{Binding Config.AverageWalkingSpeed}" Padding="3"/>

                    <Label Content="Delivery Window (HH:MM:SS):"/>
                    <TextBox Text="{Binding Config.DeliveryWindow}" Padding="3"/>

                    <Label Content="Risk Range (HH:MM:SS):"/>
                    <TextBox Text="{Binding Config.RiskRange}" Padding="3"/>

                    <Label Content="Inactive Time (HH:MM:SS):"/>
                    <TextBox Text="{Binding Config.InactiveTime}" Padding="3"/>

                </StackPanel>
            </ScrollViewer>
        </GroupBox>

    </Grid>
</Window>